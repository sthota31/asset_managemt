
<div class="container">
    <div class="row">
        <%= render "layouts/sidebar" %>
        <div class="col-sm-9 col-md-9">
          <h4>Assets</h4>
          <div style="float:right;padding:5px;">
            <%= link_to 'Import', import_assets_path ,:class=>"label label-default"%>
          </div>
          <div style="float:right;padding:5px;">
            <%= link_to 'New Asset', new_asset_path ,:class=>"label label-default"%>
          </div>
          <table class="table table-bordered">
            <thead>
             <tr>
              <th>Name</th>
              <th>Asset type</th>
              <th>Make</th>
              <th>Model</th>
              <th>Purchase Date</th>
              <th>Warranty Date</th>
              <th>Vendor</th>
              <th>AssetID</th>
              <th></th>
             </tr>
            </thead>
            <tbody>
          <% @assets.each do |asset| %>
          <tr>
            <td><%= asset.name %></td>
            <td><%= asset.asset_type %></td>
            <td><%= asset.make %></td>
            <td><%= asset.model %></td>
            <td><%= format_date(asset.purchase_date) %></td>
            <td><%= format_date(asset.warranty_date) %></td>
            <td><%= asset.vendor_name %></td>
            <td><%= asset.uniqasset_id %></td>
            <td><%= link_to 'Edit', edit_asset_path(asset) %><%= link_to 'Destroy', asset, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
          <% end %>
            </tbody>
          </table>
        </div>
    </div>
</div>

